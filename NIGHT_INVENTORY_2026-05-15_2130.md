# Mylka/Mylco — ночная инвентаризация 21:30, 2026-05-15

## Снимок состояния
- Ветка: `main`.
- Рабочее дерево уже было грязным до этой итерации: `95` изменённых HTML-файлов, плюс новые `NIGHT_FOCUS_2026-05-15.md`, `outputs/`.
- Основной характер текущих изменений по `git diff --stat`: массовое добавление небольших вставок по ~3 строки в страницы; в `3d-pechat-1/index.html` — 5 строк и 1 удаление.
- HTML-страниц в проекте: `257`.
- Визуальные проверки/скриншоты уже лежат в `outputs/mylka_images/`.

## Что уже выглядит модернизированным
- `index.html`: есть нормальный title/description и оффер «материалы для форм и литья без угадывания».
- `generated/product-pages/`: есть индекс и набор отдельных карточек/комплектов.
- `final-site.css` и `final-site.js`: есть отдельный слой финальной стилизации/поведения.
- `sitemap.html`: уже оформлен как карта preview-сайта с title/description.
- `robots.txt`: есть, но сейчас очень короткий — требует отдельной SEO-проверки.

## Где риск сломать структуру
- Массово изменены десятки legacy HTML-страниц: лучше не править их вручную россыпью без скрипта/паттерна.
- `outputs/` не отслеживается git: нельзя считать скриншоты доставленными/сохранёнными без явного решения, что их надо коммитить или архивировать.
- Старые OpenCart-страницы аккаунта/корзины/checkout (`cart.html`, `simplecheckout.html`, `my-account.html`, `wishlist.html` и т.п.) всё ещё присутствуют — их нужно либо аккуратно нейтрализовать как preview, либо не трогать до отдельной задачи.
- `generated/product-pages/index.html` пока без meta description — небольшая SEO-дырка, но не критичный первый риск.

## Приоритеты следующих 30-минутных итераций
1. **Главная:** проверить hero, CTA и мобильную шапку; убедиться, что оффер читается за 5 секунд.
2. **Legacy/preview безопасность:** понять, какие OpenCart элементы нельзя показывать как рабочие: корзина, аккаунт, checkout, сравнение, wishlist.
3. **Категории:** привести первые экраны 1–2 ключевых категорий (`kupit-silikon-v-moskve.html`, `vakuumnye-kamery.html`) к единому CTA.
4. **SEO:** проверить `robots.txt`, canonical/title/description на главной, карте сайта и generated/product-pages.
5. **Мобильная проверка:** пройти главную + ключевую категорию + карточку товара на горизонтальные переполнения и CTA.

## Безопасный следующий шаг
Начать с главной: локально открыть/проверить `index.html`, затем сделать маленькую правку только в одном месте, если найдётся очевидный дефект в CTA/мобильном первом экране.
